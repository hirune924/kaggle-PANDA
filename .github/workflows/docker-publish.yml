name: Docker-build-push

on:
  push:
    # Publish `master` as Docker `latest` image.
    branches:
      - master
    paths: 
      - docker/Dockerfile
env:
  # TODO: Change variable to your image's name.: 
  IMAGE_NAME: kaggle_panda
  DOCKERHUB_ACCOUNT: hirune924
  TAG_NAME: latest

jobs:
  # Push image to GitHub Packages.
  # See also https://docs.docker.com/docker-hub/builds/
  push:

    runs-on: ubuntu-latest
    if: github.event_name == 'push'

    steps:
      - uses: actions/checkout@v2

      - name: Build image
        run: docker build . --file docker/Dockerfile --tag $DOCKERHUB_ACCOUNT/$IMAGE_NAME

      - name: Log into registry
        env:
          DOCKER_HUB_TOKEN: ${{ secrets.DOCKER_HUB_TOKEN }}
        run: |
          echo "${DOCKER_HUB_TOKEN}" | docker login -u $DOCKERHUB_ACCOUNT --password-stdin
      - name: Push image
        run: |
          docker push $DOCKERHUB_ACCOUNT/$IMAGE_NAME:$TAG_NAME
